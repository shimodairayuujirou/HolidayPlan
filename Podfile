# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

platform :ios, '13.0' # 推奨iOSバージョン

target 'HolidayPlan' do
  use_frameworks!

  # Firebase関連のPod
  pod 'Firebase/Firestore'
  pod 'Firebase/Auth'

  target 'HolidayPlanTests' do
    inherit! :search_paths
  end

  target 'HolidayPlanUITests' do
  end
end
